---
description: 使用 ReMo 自动维护项目记忆。
alwaysApply: true
---

# ReMo Project Memory

使用 ReMo 自动维护本仓库的项目记忆。

ReMo 权威来源：`skills/remo/SKILL.md`。

项目记忆位置：

- 配置：`.remo/config.yml`
- 知识入口：`.remo/knowledge/index.md`
- 项目知识：`.remo/knowledge/`
- 过程日志：`.remo/logs/`

方法论：

- Route before read：任务开始先读 `.remo/knowledge/index.md`。
- Distill before store：保存稳定项目理解，不保存原始 transcript 或逐文件摘要。
- Evidence before trust：正式知识必须有关联证据。
- Auto-write with provenance：任务结束和 Git checkpoint 自动写入正式 knowledge，并保留来源、置信度和失效信号。
- Optimize future agent cost：目标是降低未来 Coding Agent 的上下文和返工成本。

任务开始：

- 读取 `.remo/knowledge/index.md`。
- 根据任务意图选择最小 knowledge 集合。
- 只有当 knowledge 缺失、过期、冲突或不够具体时才扫描 repo。

任务结束：

- 生成 memory delta：新增知识、更新知识、失效知识、证据、置信度和适用范围。
- 满足证据阈值时自动更新 `.remo/knowledge/`。
- 新增、重命名、废弃或改变 knowledge 用途时更新 `.remo/knowledge/index.md`。
- 记录自动写入摘要：**禁止**手写或推断 `YYYY/MM/DD/HHMM-` 前缀；**必须**运行 `sh skills/remo/scripts/remo-log-path.sh "简体中文标题"`，写入 stdout 路径（仓库机器本地 `date`，非云端/会话时区）。

Commit 和 push checkpoint：

- 提交前检查 staged diff，判断是否改变长期项目理解。
- 自动更新相关 knowledge 和 checkpoint 日志。
- 如果没有长期知识变化，在日志中说明原因，不要编造低信号知识。
- 推送后，在可获取时用 commit hash、branch、remote 和 push 结果更新日志。
- **整个 `.remo/` 目录默认纳入 Git 管理**（项目知识库：`config.yml`、`knowledge/`、`logs/`、`maps/`、`topics/` 等）。`config.yml` 的 `ignore_paths` 只表示路由/扫描时不加载日志作上下文，不等于 `.gitignore`。

## Git 提交（强制，勿等用户提醒）

以下触发任一即适用：用户说 commit / 提交 / push / 发 PR / 搞上去；或 Agent 执行任何 `git commit`。

1. **先查 ReMo**：`git status --short .remo/`
2. **有变更必 stage**：`git add .remo/`（与业务代码同批 stage，不要分两次让用户补提）
3. **commit 前必跑**：`sh skills/remo/scripts/remo-check.sh`
4. **默认同 commit**：`.remo/` 与本次业务改动进**同一个 commit**；仅当用户**明确**说「不要提交 ReMo / 只提交代码」时可例外
5. **禁止漏交**：已 stage 业务代码但 `.remo/` 仍有未 stage 变更时，**不得**执行 `git commit`

任务收尾（有意义的工作结束前）：

- 再次 `git status --short .remo/`；若本会话写过 knowledge/logs 且仍有未提交变更，而用户已要求或即将 commit/push/PR，**自动纳入**，无需用户二次提醒「记得 ReMo」
- 若用户全程未要求提交但 `.remo/` 有变更：收尾回复中列出待提交文件（兜底，不应常态发生）

正式 knowledge 要求：

- 除 `index.md` 外，`.remo/knowledge/**/*.md` 必须有 YAML frontmatter。
- 必需字段：`id`、`title`、`type`、`status`、`scope`、`confidence`、`last_verified`、`source_paths`、`evidence`、`supersedes`、`related`。
- 正文包含：Summary、When To Read、Current Knowledge、Agent Guidance、Evidence、Invalidation Signals。

如果项目带有 `skills/remo/scripts/remo-check.sh`，提交前可运行：

```sh
sh skills/remo/scripts/remo-check.sh
```

脚本检查结构、frontmatter、索引路由、source path、Cursor rule 和 staged checkpoint 提醒。
