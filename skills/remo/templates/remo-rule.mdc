---
description: 使用 ReMo 持续更新项目记忆。
alwaysApply: true
---

# ReMo 项目记忆

使用 ReMo 保持本仓库的项目记忆最新。

ReMo 权威来源：`skills/remo/SKILL.md`。

项目记忆位置：

- 过程日志：`.remo/logs/`
- 项目知识：`.remo/knowledge/`
- 知识索引：`.remo/knowledge/index.md`

记忆语言：[首次使用前与用户确认]

方法论：

- Route before read：大范围探索代码库前先读 `.remo/knowledge/index.md`。
- Distill before store：保存稳定项目理解，而不是原始 transcript 或显而易见的代码事实。
- Evidence before trust：重要结论要关联到文件、commit、PR、日志或用户决策。
- Context ROI：只有当记忆能节省的未来上下文多于读取成本时才写入。
- SKR：每条日志和知识句子都保持高信号。

如果 `.remo/knowledge/index.md` 缺失，在大范围探索前先初始化 ReMo：

- 如果未记录记忆语言，先询问用户。
- 创建 `.remo/logs/` 和 `.remo/knowledge/`。
- 创建 `index.md`、`project-overview.md`、`architecture.md`、`domain-model.md`、`workflows.md`、`decisions.md`、`conventions.md`、`faq.md` 和 `glossary.md`。
- 从现有文档和顶层结构填入可安全确认的事实。
- 如果项目有 Git remote，检查 commit 历史，将重要历史 commit 分组为回溯日志和更准确的基线知识。不要每个 commit 创建一条日志。
- 将未知部分标记为 `Status: To be filled`，并列出具体待确认问题。
- 将每个基线知识文件加入 `index.md`。
- 使用记忆语言创建安装日志。

大范围探索代码库前：

- 如果 `.remo/knowledge/index.md` 存在，先读取它。
- 使用索引选择最小相关知识文件。
- 只有当知识库缺失、过期、互相矛盾或不够具体时才扫描代码。

结束有意义的工作前，执行 ReMo 检查：

- 本次工作是否产生了里程碑、决策、方向调整或重要检查点？如果是，更新 `.remo/logs/`。
- 本次工作是否产生了能帮助未来 Agent 避免重复发现的稳定项目知识？如果是，更新 `.remo/knowledge/`。
- 本次工作是否改变或使已有知识失效？如果是，更新相关知识文件。
- 本次工作是否新增、重命名或改变了知识文件用途？如果是，更新 `.remo/knowledge/index.md`。
- 这条记忆是否无法通过 Context ROI 判断？如果是，不要写入。

Commit 和 push checkpoint：

- 每次 commit 和 push 都必须生成或更新 ReMo 日志条目。
- 提交前，如果 staged work 改变了长期项目理解，更新受影响的 `.remo/knowledge/` 文件和 `.remo/knowledge/index.md`。
- 如果 ReMo 更新描述的是本次 commit 的工作，将它们包含在同一个 commit 中。
- 推送后，在可获取时用 commit hash、分支、remote 和 push 结果更新日志。
- 如果没有长期知识变化，在日志中说明原因，不要编造低信号知识。

日志路径使用 `.remo/logs/YYYY/MM/DD/HHMM-记忆语言标题.md` 这样的格式。日志按年/月/日分文件夹保存，文件时间戳保持数字，标题段使用选定的记忆语言。

日志文件名、日志内容和知识条目都使用选定的记忆语言，并保持来源明确、待确认问题清晰。
